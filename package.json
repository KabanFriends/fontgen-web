{
    "name": "besmoothfontgen-web",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "description": "Browser-based smooth font generator for Bedrock (Phase 1 parity core)",
    "scripts": {
        "build": "tsc -p . && tsc -p tsconfig.app.json",
        "dev": "vite",
        "web": "vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "fflate": "^0.8.2"
    },
    "devDependencies": {
        "@types/node": "^26.2.0",
        "typescript": "^5.6.0",
        "vite": "^7.0.0"
    }
}
